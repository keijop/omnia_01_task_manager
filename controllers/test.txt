CREATE TABLE tasks (
    taskid int NOT NULL AUTO_INCREMENT,
    userid int NOT NULL,
    description varchar(256) NOT NULL,
    completed bool NOT NULL,
    deadline date,
    PRIMARY KEY (taskid),
    FOREIGN KEY (userid) REFERENCES users(userid)
);

CREATE TABLE reset (
    token varchar(36) NOT NULL,
    timestamp int NOT NULL,
    email varchar(255) NOT NULL,
    PRIMARY KEY (token)
);


INSERT INTO tasks (userid, description, deadline) 
    VALUES(50, 'Feed dog', '2022-10-01');

        <% if(messages.error){ %>
            <%= messages.error %>
        <% } %> 

mysql://bd80dkx7d5k03cb8:foshgcrzhfdbhljs@r6ze0q02l4me77k3.chr7pe7iynqr.eu-west-1.rds.amazonaws.com:3306/y31vb2bkp4m7gez2